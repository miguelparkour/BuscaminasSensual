import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  base: '/BuscaminasSensual/', // Cambia esto por el nombre exacto de tu repositorio
  plugins: [react()],
});
